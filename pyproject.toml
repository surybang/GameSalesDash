[project]
name = "gamesalesdash"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "surybang", email = "hos.fabien@outlook.com" }
]
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.3",
    "openpyxl==3.1.3",
    "pandas>=3.0.3",
]

[project.scripts]
gamesalesdash = "gamesalesdash.main:main"

[build-system]
requires = ["uv_build>=0.11.14,<0.12.0"]
build-backend = "uv_build"
